//import solidityf

pragma solidity ^0.4.0;

contract simpleContract {
  // if statement
  if (a==2) {
    //code if conditional met
  } else {

  }
  //checking condition first
  while (a >= 0) {
    //code if condition met
  }

  //do run this code until !condition
  do {
    //do this code first

  } while (a >= 0)

  //for loop
  for (uint i = 0; i >= 50; i++ ) {
    //code
  }

}   



